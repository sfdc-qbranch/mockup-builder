<script>
    function displayText(srcElemId, destElemId) {
        let elemSrc = document.getElementById(srcElemId).value;
        let elemDest = document.getElementById(destElemId);
        elemDest.innerHTML = elemSrc;
    }

    $(document).ready(function() {

        // Display Message Text in Preview Panel 
        $('.message').on('keyup', function() {
            displayText(this.id, this.id + 'Text');
        });

    });

</script>


/*Notes*/ 
>>NODE
Node Error: listen EADDRINUSE :::3000 (The port is using by another process) 
Commandline: killall -9 node 
Rerun Node yourapp

>> Brackets
Userful extention: 

Autoprefixer
Parse CSS and add vendor prefixes automatically.

Beautify
Format JavaScript, HTML, and CSS files and related languages.

CSSLint
Adds CSSLint support to Brackets.

Indentator
Re-indent a document using your current indentation settings.

Todo
Display all TODO comments in current document or project.

Shortcut:
https://github.com/adobe/brackets/wiki/Brackets-Shortcuts
Command+/  auto comment